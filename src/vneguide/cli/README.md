# CLI

Owner: Người 4.

Chứa vòng lặp nhập/xuất terminal và các lệnh `/status`, `/reset`, `/quit`. CLI chỉ gọi public interface của core, không chứa business logic.

Dự kiến:

```text
__main__.py
app.py
renderer.py
```

