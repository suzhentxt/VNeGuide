__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
.venv/
venv/
.env
.env.*
!.env.example
!**/.env.local.example

# Local secret files (API keys read via *_API_KEY_FILE) — never commit
.secrets/
.idea/
.vscode/
*.log
logs/
.vneguide-memory/

.vercel

# Local research inputs; never publish source workbooks.
/data/DIALECT LEXICON_v2*.xlsx

# Local-only Claude Code / MCP config and Playwright MCP artifacts
.mcp.json

# Private SSH keys — never commit
ai-training.txt
*-private-key*
*.pem

# Local UI test screenshots
*-check.png
*-full.png
chatbot-upload-panel.png

# Local test artifacts (audio fixtures, Playwright MCP debug)
test-audio.wav
.playwright-mcp/
